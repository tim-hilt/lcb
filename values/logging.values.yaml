# loki:
#   isDefault: false
promtail:
  config:
    clients:
      - url: http://{{ .Release.Name }}-loki:3100/loki/api/v1/push
    snippets:
      extraScrapeConfigs: |
        - job_name: app
          kubernetes_sd_configs:
          - role: pod
          pipeline_stages:
          - cri: {}
          - json:
              expressions:
                time: time
                level: level
                something: counter
                message: message

